<%= link_to("新規登録", "/users/new") %>
<% @users.each do |user| %>
  <p>
    <%= user.name %>
    <%= user.age %>
    <%= user.address %>
    <%= link_to "編集", "/users/#{user.id}/edit" %>
  </p>
<% end %>
